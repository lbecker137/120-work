function setup(){
  // creates a canvas that is 600px wide by 400 px wide
  createCanvas(600, 600);
  // colors the background cyan
  background( 'lightblue');
}

function draw(){
  // uses the text function to write "Hello world"
  text("Hello World!",270,40);
}
